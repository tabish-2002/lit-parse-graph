import { ProteinInteractionPlatform } from "@/components/ProteinInteractionPlatform";

const Index = () => {
  return (
    <div className="min-h-screen bg-background">
      <ProteinInteractionPlatform />
    </div>
  );
};

export default Index;